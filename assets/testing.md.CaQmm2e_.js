import{_ as a,o as n,c as e,ag as t}from"./chunks/framework.DM0yugQT.js";const h=JSON.parse('{"title":"Testsuite","description":"","frontmatter":{},"headers":[],"relativePath":"testing.md","filePath":"testing.md"}'),p={name:"testing.md"};function i(o,s,l,r,c,d){return n(),e("div",null,[...s[0]||(s[0]=[t(`<h1 id="testsuite" tabindex="-1">Testsuite <a class="header-anchor" href="#testsuite" aria-label="Permalink to &quot;Testsuite&quot;">​</a></h1><p>OSRM comes with a testsuite containing both unit-tests using the Boost library and cucumber.js for scenario driven testing.</p><h2 id="unit-tests" tabindex="-1">Unit Tests <a class="header-anchor" href="#unit-tests" aria-label="Permalink to &quot;Unit Tests&quot;">​</a></h2><p>For a general introduction on Boost.Test have a look at <a href="http://www.boost.org/doc/libs/1_60_0/libs/test/doc/html/index.html" target="_blank" rel="noreferrer">its docs</a>.</p><h3 id="separate-test-binaries" tabindex="-1">Separate Test Binaries <a class="header-anchor" href="#separate-test-binaries" aria-label="Permalink to &quot;Separate Test Binaries&quot;">​</a></h3><p>Unit tests should be registered according to the sub-project they&#39;re in. If you want to write tests for utility functions, add them to the utility test binary. See <code>CMakeLists.txt</code> in the unit test directory for how to register new unit tests.</p><h3 id="using-boost-test-primitives" tabindex="-1">Using Boost.Test Primitives <a class="header-anchor" href="#using-boost-test-primitives" aria-label="Permalink to &quot;Using Boost.Test Primitives&quot;">​</a></h3><p>There is a difference between only reporting a failed condition and aborting the test right at a failed condition. Have a look at <a href="http://www.boost.org/doc/libs/1_60_0/libs/test/doc/html/boost_test/utf_reference/testing_tool_ref/assertion_boost_level.html" target="_blank" rel="noreferrer"><code>BOOST_CHECK</code> vs <code>BOOST_REQUIRE</code></a>. Instead of manually checking e.g. for equality, less than, if a function throws etc. use their <a href="http://www.boost.org/doc/libs/1_60_0/libs/test/doc/html/boost_test/utf_reference/testing_tool_ref.html" target="_blank" rel="noreferrer">corresponding Boost.Test primitives</a>.</p><p>If you use <code>BOOST_CHECK_EQUAL</code> you have to implement <code>operator&lt;&lt;</code> for your type so that Boost.Test can print mismatches. If you do not want to do this, define <code>BOOST_TEST_DONT_PRINT_LOG_VALUE</code> (and undef it after the check call) or sidestep it with <code>BOOST_CHECK(fst == snd);</code>.</p><h3 id="test-fixture" tabindex="-1">Test Fixture <a class="header-anchor" href="#test-fixture" aria-label="Permalink to &quot;Test Fixture&quot;">​</a></h3><p>If you need to test features on a real dataset (think about this twice: prefer cucumber and dataset-independent tests for their reproducibility and minimality), there is a fixed dataset in <code>test/data</code>. This dataset is a small extract and may not even contain all tags or edge cases. Furthermore this dataset is not in sync with what you see in up-to-date OSM maps or on the demo server. See the library tests for how to add new dataset dependent tests.</p><p>To prepare the test data simply <code>cd test/data/</code> and then run <code>make</code>.</p><h3 id="running-tests" tabindex="-1">Running Tests <a class="header-anchor" href="#running-tests" aria-label="Permalink to &quot;Running Tests&quot;">​</a></h3><p>To build the unit tests:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>cd build</span></span>
<span class="line"><span>cmake ..</span></span>
<span class="line"><span>make tests</span></span></code></pre></div><p>You should see the compiled binaries in <code>build/unit_tests</code>, you can then run each suite individually:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>./engine-tests</span></span></code></pre></div><h2 id="cucumber" tabindex="-1">Cucumber <a class="header-anchor" href="#cucumber" aria-label="Permalink to &quot;Cucumber&quot;">​</a></h2><p>For a general introduction on cucumber in our testsuite, have a look at <a href="https://github.com/Project-OSRM/osrm-backend/wiki/Cucumber-Test-Suite" target="_blank" rel="noreferrer">the wiki</a>.</p><p>This documentation aims to supply a guideline on how to write cucumber tests that test new features introduced into osrm.</p><h3 id="test-the-feature" tabindex="-1">Test the feature <a class="header-anchor" href="#test-the-feature" aria-label="Permalink to &quot;Test the feature&quot;">​</a></h3><p>It is often tempting to reduce the test to a path and accompanying instructions. Instructions can and will change over the course of improving guidance.</p><p>Instructions should only be used when writing a feature located in <code>features/guidance</code>. All other features should avoid using instructions at all.</p><h3 id="write-tests-to-scale" tabindex="-1">Write Tests to Scale <a class="header-anchor" href="#write-tests-to-scale" aria-label="Permalink to &quot;Write Tests to Scale&quot;">​</a></h3><p>OSRM is a navigation engine. Tests should always consider this context.</p><p>An important implication is the grid size. If tests use a very small grid size, you run into the chance of instructions being omitted. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Background:</span></span>
<span class="line"><span>    Given the profile &quot;car&quot;</span></span>
<span class="line"><span>    Given a grid size of 10 meters</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Scenario: Testbot - Straight Road</span></span>
<span class="line"><span>    Given the node map</span></span>
<span class="line"><span>        &quot;&quot;&quot;</span></span>
<span class="line"><span>        a b c d</span></span>
<span class="line"><span>        &quot;&quot;&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    And the ways</span></span>
<span class="line"><span>        | nodes | highway |</span></span>
<span class="line"><span>        | ab    | primary |</span></span>
<span class="line"><span>        | bc    | primary |</span></span>
<span class="line"><span>        | cd    | primary |</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    When I route I should get</span></span>
<span class="line"><span>        | from | to | route       |</span></span>
<span class="line"><span>        | a    | d  | ab,bc,cd,cd |</span></span></code></pre></div><p>In a navigation engine, the instructions</p><ul><li>depart east on ab</li><li>in 10 meters the road name changes to bc</li><li>in 10 meters the road name changes to cd</li><li>you arrived at cd</li></ul><p>would be impossible to announce and not helpful at all. Since no actual choices exist, the route you get could result in <code>ab,cd</code> and simply say <code>depart</code> and <code>arrive</code>.</p><p>To prevent such surprises, always consider the availability of other roads and use grid sizes/road lengths that correspond to actually reasonable scenarios in a road network.</p><h3 id="use-names" tabindex="-1">Use names <a class="header-anchor" href="#use-names" aria-label="Permalink to &quot;Use names&quot;">​</a></h3><p>If you specify many nodes in close succession to present a specific road geometry, consider using <code>name</code> to indicate to OSRM that the segment is a single road.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Background:</span></span>
<span class="line"><span>    Given the profile &quot;car&quot;</span></span>
<span class="line"><span>    Given a grid size of 10 meters</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Scenario: Testbot - Straight Road</span></span>
<span class="line"><span>    Given the node map</span></span>
<span class="line"><span>        &quot;&quot;&quot;</span></span>
<span class="line"><span>        a b c d</span></span>
<span class="line"><span>        &quot;&quot;&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    And the ways</span></span>
<span class="line"><span>        | nodes | highway | name |</span></span>
<span class="line"><span>        | ab    | primary | road |</span></span>
<span class="line"><span>        | bc    | primary | road |</span></span>
<span class="line"><span>        | cd    | primary | road |</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    When I route I should get</span></span>
<span class="line"><span>        | from | to | route     | turns         |</span></span>
<span class="line"><span>        | a    | d  | road,road | depart,arrive |</span></span></code></pre></div><p>Guidance guarantees only essential maneuvers. You will always see <code>depart</code> and <code>arrive</code> as well as all turns that are not obvious.</p><p>So the following scenario does not change the instructions</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Background:</span></span>
<span class="line"><span>    Given the profile &quot;car&quot;</span></span>
<span class="line"><span>    Given a grid size of 10 meters</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Scenario: Testbot - Straight Road</span></span>
<span class="line"><span>    Given the node map</span></span>
<span class="line"><span>        &quot;&quot;&quot;</span></span>
<span class="line"><span>        a b</span></span>
<span class="line"><span>        d c</span></span>
<span class="line"><span>        &quot;&quot;&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    And the ways</span></span>
<span class="line"><span>        | nodes | highway | name |</span></span>
<span class="line"><span>        | ab    | primary | road |</span></span>
<span class="line"><span>        | bc    | primary | road |</span></span>
<span class="line"><span>        | cd    | primary | road |</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    When I route I should get</span></span>
<span class="line"><span>        | from | to | route     | turns         |</span></span>
<span class="line"><span>        | a    | d  | road,road | depart,arrive |</span></span></code></pre></div><p>but if we modify it to</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Background:</span></span>
<span class="line"><span>    Given the profile &quot;car&quot;</span></span>
<span class="line"><span>    Given a grid size of 10 meters</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Scenario: Testbot - Straight Road</span></span>
<span class="line"><span>    Given the node map</span></span>
<span class="line"><span>        &quot;&quot;&quot;</span></span>
<span class="line"><span>        a b e</span></span>
<span class="line"><span>        d c</span></span>
<span class="line"><span>        &quot;&quot;&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    And the ways</span></span>
<span class="line"><span>        | nodes | highway | name |</span></span>
<span class="line"><span>        | ab    | primary | road |</span></span>
<span class="line"><span>        | bc    | primary | road |</span></span>
<span class="line"><span>        | cd    | primary | road |</span></span>
<span class="line"><span>        | be    | primary | turn |</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    When I route I should get</span></span>
<span class="line"><span>        | from | to | route          | turns                        |</span></span>
<span class="line"><span>        | a    | d  | road,road,road | depart,continue right,arrive |</span></span></code></pre></div><h3 id="test-all-directions" tabindex="-1">Test all directions <a class="header-anchor" href="#test-all-directions" aria-label="Permalink to &quot;Test all directions&quot;">​</a></h3><p>Modelling a road as roundabout has an implied oneway tag associated with it. In the following case, we can route from <code>a</code> to <code>d</code> but not from <code>d</code> to <code>a</code>. To discover those errors, make sure to check for all allowed directions.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Scenario: Enter and Exit mini roundabout with sharp angle   # features/guidance/mini-roundabout.feature:37</span></span>
<span class="line"><span>    Given the profile &quot;car&quot;                                   # features/step_definitions/data.js:8</span></span>
<span class="line"><span>    Given a grid size of 10 meters                            # features/step_definitions/data.js:20</span></span>
<span class="line"><span>    Given the node map                                        # features/step_definitions/data.js:45</span></span>
<span class="line"><span>        &quot;&quot;&quot;</span></span>
<span class="line"><span>        a b</span></span>
<span class="line"><span>          c d</span></span>
<span class="line"><span>        &quot;&quot;&quot;</span></span>
<span class="line"><span>    And the ways                                              # features/step_definitions/data.js:128</span></span>
<span class="line"><span>        | nodes | highway         | name |</span></span>
<span class="line"><span>        | ab    | tertiary        | MySt |</span></span>
<span class="line"><span>        | bc    | roundabout      |      |</span></span>
<span class="line"><span>        | cd    | tertiary        | MySt |</span></span>
<span class="line"><span>    When I route I should get                                 # features/step_definitions/routing.js:4</span></span>
<span class="line"><span>        | from | to | route     | turns         | #                                               |</span></span>
<span class="line"><span>        | a    | d  | MySt,MySt | depart,arrive | # suppress multiple enter/exit mini roundabouts |</span></span>
<span class="line"><span>        | d    | a  | MySt,MySt | depart,arrive | # suppress multiple enter/exit mini roundabouts |</span></span>
<span class="line"><span>    Tables were not identical:</span></span>
<span class="line"><span>        |  from |     to |     route     |     turns         |     #</span></span>
<span class="line"><span>        |     a |      d |     MySt,MySt |     depart,arrive |     # suppress multiple enter/exit mini roundabouts |</span></span>
<span class="line"><span>        | (-) d |  (-) a | (-) MySt,MySt | (-) depart,arrive | (-) # suppress multiple enter/exit mini roundabouts |</span></span>
<span class="line"><span>        | (+) d |  (+) a | (+)           | (+)               | (+) # suppress multiple enter/exit mini roundabouts |</span></span></code></pre></div><h3 id="prevent-randomness" tabindex="-1">Prevent Randomness <a class="header-anchor" href="#prevent-randomness" aria-label="Permalink to &quot;Prevent Randomness&quot;">​</a></h3><p>Some features in OSRM can result in strange experiences during testcases. To prevent some of these issues, follow the guidelines below.</p><h4 id="use-waypoints" tabindex="-1">Use Waypoints <a class="header-anchor" href="#use-waypoints" aria-label="Permalink to &quot;Use Waypoints&quot;">​</a></h4><p>Using grid nodes as waypoints offers the chance of unwanted side effects. OSRM converts the grid into a so called edge-based graph.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Scenario: Testbot - Intersection</span></span>
<span class="line"><span>    Given the node map</span></span>
<span class="line"><span>        &quot;&quot;&quot;</span></span>
<span class="line"><span>          e</span></span>
<span class="line"><span>        b a d</span></span>
<span class="line"><span>          c</span></span>
<span class="line"><span>        &quot;&quot;&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    And the ways</span></span>
<span class="line"><span>        | nodes | highway | oneway |</span></span>
<span class="line"><span>        | ab    | primary | yes    |</span></span>
<span class="line"><span>        | ac    | primary | yes    |</span></span>
<span class="line"><span>        | ad    | primary | yes    |</span></span>
<span class="line"><span>        | ae    | primary | yes    |</span></span></code></pre></div><p>Selecting <code>a</code> as a <code>waypoint</code> results in four possible starting locations. Which one of the routes <code>a,b</code>, <code>a,c</code>, <code>a,d</code>, or <code>a,e</code> is found is pure chance and depends on the order in the static <code>r-tree</code>.</p><p>To guarantee discovery, use:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Scenario: Testbot - Intersection</span></span>
<span class="line"><span>    Given the node map</span></span>
<span class="line"><span>        &quot;&quot;&quot;</span></span>
<span class="line"><span>            e</span></span>
<span class="line"><span>            4</span></span>
<span class="line"><span>        b 1 a 3 d</span></span>
<span class="line"><span>            2</span></span>
<span class="line"><span>            c</span></span>
<span class="line"><span>        &quot;&quot;&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    And the ways</span></span>
<span class="line"><span>        | nodes | highway | oneway |</span></span>
<span class="line"><span>        | ab    | primary | yes    |</span></span>
<span class="line"><span>        | ac    | primary | yes    |</span></span>
<span class="line"><span>        | ad    | primary | yes    |</span></span>
<span class="line"><span>        | ae    | primary | yes    |</span></span></code></pre></div><p>And use <code>1</code>,<code>2</code>,<code>3</code>, and <code>4</code> as starting waypoints. The routes <code>1,b</code>, <code>2,c</code>, <code>3,d</code>, and <code>4,e</code> can all be discovered.</p><h4 id="allow-for-small-offsets" tabindex="-1">Allow For Small Offsets <a class="header-anchor" href="#allow-for-small-offsets" aria-label="Permalink to &quot;Allow For Small Offsets&quot;">​</a></h4><p>Whenever you are independent of the start location (see use waypoints), the waypoint chosen as start/end location can still influence distances/durations.</p><p>If you are testing for a duration metric, allow for a tiny offset to ensure a passing test in the presence of rounding/snapping issues.</p><h4 id="don-t-rely-on-alternatives" tabindex="-1">Don&#39;t Rely on Alternatives <a class="header-anchor" href="#don-t-rely-on-alternatives" aria-label="Permalink to &quot;Don&#39;t Rely on Alternatives&quot;">​</a></h4><p>Alternative route discovery is a random feature in itself. The discovery of routes depends on the contraction order of roads and cannot be assumed successful, ever.</p><h3 id="understanding-turn-restrictions" tabindex="-1">Understanding Turn Restrictions <a class="header-anchor" href="#understanding-turn-restrictions" aria-label="Permalink to &quot;Understanding Turn Restrictions&quot;">​</a></h3><p>Adding turn restrictions requires the restriction to follow a very specific format.</p><p>We specify them in a table with the header <code>| type | way:from | way:to | node:via | restriction |</code>. It is important that turn restrictions require micro segmentation.</p><p>Consider the following scenario:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Given the node map:</span></span>
<span class="line"><span>    &quot;&quot;&quot;</span></span>
<span class="line"><span>          e</span></span>
<span class="line"><span>          |</span></span>
<span class="line"><span>    a - - b - - c</span></span>
<span class="line"><span>          |</span></span>
<span class="line"><span>          d</span></span>
<span class="line"><span>    &quot;&quot;&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>And the ways</span></span>
<span class="line"><span>    | nodes | oneway |</span></span>
<span class="line"><span>    | abc   | yes    |</span></span>
<span class="line"><span>    | ebd   | yes    |</span></span>
<span class="line"><span></span></span>
<span class="line"><span>And the relations</span></span>
<span class="line"><span>    | type        | way:from | way:to | node:via | restriction   |</span></span>
<span class="line"><span>    | restriction | abc      | ebd    | b        | no_right_turn |</span></span></code></pre></div><p>The setting looks perfectly fine at first glance. However, it is not well defined. The forbidden right turn could be either a superfluous addition, forbidding the turn <code>cb</code> to <code>be</code>, or actually refer to the turn <code>ab</code> to <code>bd</code> to say that a turn is forbidden here.</p><p>To model turn-restrictions correctly and uniquely, we need to split segments that contribute to the restriction into the smallest possible parts. E.g. the above scenario could correctly be expressed as:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Given the node map:</span></span>
<span class="line"><span>    &quot;&quot;&quot;</span></span>
<span class="line"><span>          e</span></span>
<span class="line"><span>          |</span></span>
<span class="line"><span>    a - - b - - c</span></span>
<span class="line"><span>          |</span></span>
<span class="line"><span>          d</span></span>
<span class="line"><span>    &quot;&quot;&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>And the ways</span></span>
<span class="line"><span>    | nodes | oneway | name |</span></span>
<span class="line"><span>    | ab    | yes    | abc  |</span></span>
<span class="line"><span>    | bc    | yes    | abc  |</span></span>
<span class="line"><span>    | eb    | yes    | ebd  |</span></span>
<span class="line"><span>    | bd    | yes    | ebd  |</span></span>
<span class="line"><span></span></span>
<span class="line"><span>And the relations</span></span>
<span class="line"><span>    | type        | way:from | way:to | node:via | restriction   |</span></span>
<span class="line"><span>    | restriction | ab       | bd     | b        | no_right_turn |</span></span></code></pre></div><p>Unless this format is used, OSRM will omit the (then ambiguous) turn restrictions and ignore them.</p><h2 id="my-guidance-tests-are-failing-understanding-what-you-can-change" tabindex="-1">My Guidance Tests are Failing - Understanding what you can change <a class="header-anchor" href="#my-guidance-tests-are-failing-understanding-what-you-can-change" aria-label="Permalink to &quot;My Guidance Tests are Failing - Understanding what you can change&quot;">​</a></h2><p>If you change some stuff in guidance, you will easily see tests change their result. E.g. if you change the angles for which we report <code>right</code>, then obviously some tests might not report a <code>direction modifier</code> named <code>right</code> anymore.</p><p>This small section will try to guide you in making the correct decisions for changing the behaviour of tests.</p><p>The difficulty in guidance tests is that not all items can be translated 1:1 from the ascii art into turn-angles.</p><p>The turn-angle calculation tries to find turn angles that would represent perceived turn angles, not the exact angle at the connection.</p><p>This is necessary, since connections in OSM are always bound by the paradigm that the way is supposed to be in the middle of the actual road. For broad streets, you will see stronger angles than the actual turns.</p><h3 id="don-t-change-the-test-change-the-expected-behaviour" tabindex="-1">Don&#39;t change the test, change the expected behaviour <a class="header-anchor" href="#don-t-change-the-test-change-the-expected-behaviour" aria-label="Permalink to &quot;Don&#39;t change the test, change the expected behaviour&quot;">​</a></h3><p>If we have a test that looks like this:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Given a grid size of 5 m</span></span>
<span class="line"><span>Given the node map</span></span>
<span class="line"><span>&quot;&quot;&quot;</span></span>
<span class="line"><span>a - b - - - - - - c</span></span>
<span class="line"><span>     \\  </span></span>
<span class="line"><span>      d - - - - - e</span></span>
<span class="line"><span>&quot;&quot;&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>When I route I should get</span></span>
<span class="line"><span> | waypoints | route       | turns                          |</span></span>
<span class="line"><span> | a,e       | abc,bde,bde | depart,turn slight right,arrive|</span></span></code></pre></div><p>And the test reports <code>turn right</code> for the route <code>a-&gt;e</code>, where before it said <code>slight right</code>.</p><p>If you change the turn angles, obviously you can expect changes in the distinction between <code>slight right</code> and <code>right</code>. In such a case it is, of course, reasonable to change the expected route to report <code>right</code> instead of <code>slight right</code>. You should consider inspecting the actual turn angles at <code>b</code> to see if you feel that change is justified.</p><p>However, you should never adjust the test itself. If you look at a failure, the other way around</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Given a grid size of 5 m</span></span>
<span class="line"><span>Given the node map</span></span>
<span class="line"><span>&quot;&quot;&quot;</span></span>
<span class="line"><span>a - b - - - - - - c</span></span>
<span class="line"><span>     \\  </span></span>
<span class="line"><span>      d - - - - - e</span></span>
<span class="line"><span>&quot;&quot;&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>When I route I should get</span></span>
<span class="line"><span> | waypoints | route       | turns                   |</span></span>
<span class="line"><span> | a,e       | abc,bde,bde | depart,turn right,arrive|</span></span></code></pre></div><p>where we see a <code>slight right</code>, over the expected <code>right</code>. We could be tempted to adjust the grid size (e.g. from <code>10 m</code> to <code>20</code> meters).</p><p>Such a change would fundamentally alter the tests, though. Since the part <code>b-d</code> is a short offset, when we are looking at a grid of size <code>5 m</code>, the angle calculation will try and compensate for this offset.</p><p>In this case we would see a very slight turn angle. If your change now reports different turn angles, you can of course change the expected result. But you should not adjust the grid size. The test would be testing turn angles of <code>180</code> and <code>100</code> degrees, instead of <code>180</code> and <code>160</code>.</p><h3 id="consider-post-processing-impacts" tabindex="-1">Consider Post-Processing Impacts <a class="header-anchor" href="#consider-post-processing-impacts" aria-label="Permalink to &quot;Consider Post-Processing Impacts&quot;">​</a></h3><p>Some changes you might see could look completely unrelated. To understand the impact of your changes, you can make use of the debugging utilities you can find in <code>util/debug.hpp</code> (and potentially other related headers).</p><p>If your test is inspecting a series of turns (remember, a turn does not necessarily equals an instruction), you could see interaction with post-processing. To see the unprocessed turns, you should print the steps at the end of step assembly (<code>assembleSteps</code> in <code>engine/guidance/assemble_steps.hpp</code>).</p><p>If you see unexpected changes, you can consider adding the <code>locations</code> field to your test to study what location a turn is reported at.</p><p>To study a test without post-processing impacts, you can create a copy of the case on a very large grid (like 2000 meters). In such a grid, <code>turn collapsing</code> would be essentially disabled.</p><p>Sadly, there is no general guideline.</p><h3 id="use-caution" tabindex="-1">Use Caution <a class="header-anchor" href="#use-caution" aria-label="Permalink to &quot;Use Caution&quot;">​</a></h3><p>If in doubt, ask another person. Inspect as much of the data as possible (e.g. print un-collapsed steps, turn angles and so on) and use your best judgement, if the new result seems justified.</p>`,89)])])}const g=a(p,[["render",i]]);export{h as __pageData,g as default};
