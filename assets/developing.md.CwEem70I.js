import{_ as o,o as t,c as i,ag as a}from"./chunks/framework.DM0yugQT.js";const p=JSON.parse('{"title":"Developing / Debugging guidance code","description":"","frontmatter":{},"headers":[],"relativePath":"developing.md","filePath":"developing.md"}'),n={name:"developing.md"};function r(d,e,g,s,c,u){return t(),i("div",null,[...e[0]||(e[0]=[a('<h1 id="developing-debugging-guidance-code" tabindex="-1">Developing / Debugging guidance code <a class="header-anchor" href="#developing-debugging-guidance-code" aria-label="Permalink to &quot;Developing / Debugging guidance code&quot;">​</a></h1><p>When changing guidance code, it is easy to introduce problems somewhere in the network. To get a better feeling of how your changes impact the OSRM experience, we offer ways of generating geojson output to inspect (e.g. with Mapbox Studio). When you do changes, make sure to inspect a few areas for the impact of the changes.</p><h2 id="how-to-use-geojson-debugging" tabindex="-1">How to use GeoJson-Debugging <a class="header-anchor" href="#how-to-use-geojson-debugging" aria-label="Permalink to &quot;How to use GeoJson-Debugging&quot;">​</a></h2><p>This is a short guide to describe usage of our GeoJson debug logging mechanism. It is synchronized to guarantee thread-safe logging.</p><h2 id="outputting-into-a-single-file" tabindex="-1">Outputting into a single file <a class="header-anchor" href="#outputting-into-a-single-file" aria-label="Permalink to &quot;Outputting into a single file&quot;">​</a></h2><p>To use it, the inclusion of <code>geojson_debug_logger.hpp</code> <code>geojson_debug_policies.hpp</code> from the <code>util</code> directory is required.</p><p>Geojson debugging requires a few simple steps to output data into a feature collection.</p><ul><li>Create a Scoped Guard that lives through the process and provide it with all required datastructures (it needs to span the lifetime of all your logging efforts)</li><li>At the location of the output, simply call Write with your own parameters.</li></ul><p>A guard (ScopedGeojsonLoggerGuard) requires a logging policy. Per default we provide a way of printing out node-ids as coordinates.</p><p>The initialisation to do so looks like this: <code>util::ScopedGeojsonLoggerGuard&lt;util::NodeIdVectorToLineString&gt; geojson_guard( &quot;debug.geojson&quot;, data-for-conversion);</code> Make sure to give the guard a name, so it actually gets a lifetime.</p><p>The field <code>data-for-conversion</code> can be an arbitrary long set of features and needs to match the parameters used for constructing our policy (in this case <code>util::NodeIdVectorToLineString</code>).</p><p>The policy itself offers a <code>operator()</code> accepting a <code>vector</code> of <code>NodeID</code>.</p><p>For outputting data into our file (debug.geojson), we simply need to call the matching logging routine of the guard: <code>util::ScioedGeojsonLoggerGuard&lt;util::NodeIdVectorToLineString&gt;::Write(list_of_node_ids);</code> (or <code>guard.Write(list_of_node_ids)</code> if you created an instance).</p><h3 id="possible-scopeguard-location" tabindex="-1">Possible Scopeguard Location <a class="header-anchor" href="#possible-scopeguard-location" aria-label="Permalink to &quot;Possible Scopeguard Location&quot;">​</a></h3><p>Think of the scopeguard as you would do of any reference. If you want to access logging during a call, the guard object must be alive and valid.</p><p>As an example: a good location to create a scopeguard to log decisions in the edge-based-graph-factory would be right before we run it (<a href="https://github.com/Project-OSRM/osrm-backend/blob/a933b5d94943bf3edaf42c84a614a99650d23cba/src/extractor/extractor.cpp#L497" target="_blank" rel="noreferrer">here</a>). If you put <code>util::ScopedGeojsonLoggerGuard&lt;util::NodeIdVectorToLineString&gt; geojson_guard( &quot;debug.geojson&quot;, node_coordinate_vector);</code> at that location, you can then print <code>util::ScopedGeojsonLoggerGuard&lt;util::NodeIdVectorToLineString&gt;::Write(list_of_node_ids);</code> anywhere within the <code>edge-based-graph-factory</code>.</p><p>This location would enable calls for all guidance related pre-processing which is called in the edge-based-graph-factory. Logging any turn-handler decisions, for example, would now be possible.</p><h2 id="limitations" tabindex="-1">Limitations <a class="header-anchor" href="#limitations" aria-label="Permalink to &quot;Limitations&quot;">​</a></h2><p>GeoJson debugging requires a single GeoJsonGuard (ScopedGeojsonLoggerGuard) for each desired output file. For each set of template parameters, only the most recent guard will actually produce output.</p><p><code>util::ScopedGeojsonLoggerGuard&lt;util::NodeIdVectorToLineString&gt; geojson_guard( &quot;debug.geojson&quot;, data-for-conversion);</code></p><p><code>util::ScopedGeojsonLoggerGuard&lt;util::NodeIdVectorToLineString&gt; geojson_guard( &quot;debug-2.geojson&quot;, data-for-conversion);</code></p><p>Will not provide a way to write into two files, but only <code>debug-2</code> will actually contain features.</p><p>We cannot nest these calls.</p><p>If we want to use the same policy for multiple files, we need to use different template parameters both for the logger and the guard.</p><p><code>util::ScopedGeojsonLoggerGuard&lt;util::NodeIdVectorToLineString,0&gt; geojson_guard( &quot;debug.geojson&quot;, data-for-conversion);</code></p><p><code>util::ScopedGeojsonLoggerGuard&lt;util::NodeIdVectorToLineString,1&gt; geojson_guard( &quot;debug-2.geojson&quot;, data-for-conversion);</code></p><p>as well as,</p><p><code>util::ScopedGeojsonLoggerGuardr&lt;util::NodeIdVectorToLineString,0&gt;::Write(list_of_node_ids);</code></p><p><code>util::ScopedGeojsonLoggerGuardr&lt;util::NodeIdVectorToLineString,1&gt;::Write(list_of_node_ids);</code></p>',29)])])}const h=o(n,[["render",r]]);export{p as __pageData,h as default};
