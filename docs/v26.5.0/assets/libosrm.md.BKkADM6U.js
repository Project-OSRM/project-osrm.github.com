import{_ as t,o,c as a,ag as r}from"./chunks/framework.C2NLiVii.js";const p=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"libosrm.md","filePath":"libosrm.md"}'),i={name:"libosrm.md"};function n(s,e,c,d,l,h){return o(),a("div",null,[...e[0]||(e[0]=[r('<h2 id="introduction" tabindex="-1">Introduction <a class="header-anchor" href="#introduction" aria-label="Permalink to &quot;Introduction&quot;">​</a></h2><p>OSRM can be used as a library (libosrm) via C++ instead of using it through the HTTP interface and <code>osrm-routed</code>. This allows for fine-tuning OSRM and has much less overhead. Here is a quick introduction into how to use <code>libosrm</code> in the current version.</p><p>Take a look at the example code that lives in the <a href="https://github.com/Project-OSRM/osrm-backend/tree/master/example" target="_blank" rel="noreferrer">example directory</a>. Here is all you ever wanted to know about <code>libosrm</code>, that is a short description of what the types do and where to find documentation on it:</p><h2 id="important-interface-objects" tabindex="-1">Important interface objects <a class="header-anchor" href="#important-interface-objects" aria-label="Permalink to &quot;Important interface objects&quot;">​</a></h2><ul><li><p><a href="https://github.com/Project-OSRM/osrm-backend/blob/master/include/engine/engine_config.hpp" target="_blank" rel="noreferrer"><code>EngineConfig</code></a> - for initializing an OSRM instance we can configure certain properties and constraints. E.g. the storage config is the base path such as <code>france.osm.osrm</code> from which we derive and load <code>france.osm.osrm.*</code> auxiliary files. This also lets you set constraints such as the maximum number of locations allowed for specific services.</p></li><li><p><a href="https://github.com/Project-OSRM/osrm-backend/blob/master/include/osrm/osrm.hpp" target="_blank" rel="noreferrer"><code>OSRM</code></a> - this is the main Routing Machine type with functions such as <code>Route</code> and <code>Table</code>. You initialize it with a <code>EngineConfig</code>. It does all the heavy lifting for you. Each function takes its own parameters, e.g. the <code>Route</code> function takes <code>RouteParameters</code>, and a out-reference to a JSON result that gets filled. The return value is a <code>Status</code>, indicating error or success.</p></li><li><p><a href="https://github.com/Project-OSRM/osrm-backend/blob/master/include/engine/status.hpp" target="_blank" rel="noreferrer"><code>Status</code></a> - this is a type wrapping <code>Error</code> or <code>Ok</code> for indicating error or success, respectively.</p></li><li><p><a href="https://github.com/Project-OSRM/osrm-backend/blob/master/include/engine/api/table_parameters.hpp" target="_blank" rel="noreferrer"><code>TableParameters</code></a> - this is an example of parameter types the Routing Machine functions expect. In this case <code>Table</code> expects its own parameters as <code>TableParameters</code>. You can see it wrapping two vectors, sources and destinations --- these are indices into your coordinates for the table service to construct a matrix from (empty sources or destinations means: use all of them). If you ask yourself where coordinates come from, you can see <code>TableParameters</code> inheriting from <code>BaseParameters</code>.</p></li><li><p><a href="https://github.com/Project-OSRM/osrm-backend/blob/master/include/engine/api/base_parameters.hpp" target="_blank" rel="noreferrer"><code>BaseParameters</code></a> - this most importantly holds coordinates (and a few other optional properties that you don&#39;t need for basic usage); the specific parameter types inherit from <code>BaseParameters</code> to get these member attributes. That means your <code>TableParameters</code> type has <code>coordinates</code>, <code>sources</code> and <code>destinations</code> member attributes (and a few other that we ignore for now).</p></li><li><p><a href="https://github.com/Project-OSRM/osrm-backend/blob/master/include/util/coordinate.hpp" target="_blank" rel="noreferrer"><code>Coordinate</code></a> - this is a wrapper around a (longitude, latitude) pair. We really don&#39;t care about (lon, lat) vs (lat, lon) but we don&#39;t want you to accidentally mix them up, so both latitude and longitude are strictly typed wrappers around integers (fixed notation such as <code>13423240</code>) and floating points (floating notation such as <code>13.42324</code>).</p></li><li><p><a href="https://github.com/Project-OSRM/osrm-backend/tree/master/include/engine/api" target="_blank" rel="noreferrer">Parameters for other services</a> - here are all other <code>*Parameters</code> you need for other Routing Machine services.</p></li><li><p><a href="https://github.com/Project-OSRM/osrm-backend/blob/master/include/util/json_container.hpp" target="_blank" rel="noreferrer">JSON</a> - this is a sum type resembling JSON. The Routing Machine service functions take a out-ref to a JSON result and fill it accordingly. It is currently implemented using <code>std::variant</code>. There are two ways to work with this sum type: either provide a visitor that acts on each type on visitation (with <code>std::visit</code>) or use the <code>std::get</code> function in case you&#39;re sure about the structure. The JSON structure is written down in the <a href="#http-api">HTTP API</a>.</p></li></ul><h2 id="example" tabindex="-1">Example <a class="header-anchor" href="#example" aria-label="Permalink to &quot;Example&quot;">​</a></h2><p>See <a href="https://github.com/Project-OSRM/osrm-backend/tree/master/example" target="_blank" rel="noreferrer">the example folder</a> in the OSRM repository.</p><h2 id="workflow" tabindex="-1">Workflow <a class="header-anchor" href="#workflow" aria-label="Permalink to &quot;Workflow&quot;">​</a></h2><ul><li>Create an <code>OSRM</code> instance initialized with a <code>EngineConfig</code></li><li>Call the service function on the <code>OSRM</code> object providing service specific <code>*Parameters</code></li><li>Check the return code and use the JSON result</li></ul>',9)])])}const m=t(i,[["render",n]]);export{p as __pageData,m as default};
